/* Off-target UDS client tests (NFR-4): request byte layout, negative
 * response/NRC decode, session state tracking. Built for the host, not the
 * target MCU. */
#include "uds_client.h"

int main(void)
{
    return 0;
}
