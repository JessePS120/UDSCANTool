# Fixtures

Canned byte-sequence fixtures for the off-target tests in `../isotp/` and
`../uds_client/` (spec §5.1): exactly-7-byte single frame, 8-byte message
forcing multi-frame, multi-frame with non-default block size/STmin, and
negative-response (0x7F) cases.
