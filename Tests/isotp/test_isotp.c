/* Off-target ISO-TP framing/reassembly tests (NFR-4, spec §5.1): single
 * frame, first/consecutive frame, flow control, non-default block
 * size/STmin. Built for the host, not the target MCU. */
#include "isotp.h"

int main(void)
{
    return 0;
}
